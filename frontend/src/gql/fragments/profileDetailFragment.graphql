fragment ProfileDetailFragment on ProfileType {
  id 
  user {
    id
    username
  }
  following {
    id
    user {
      id
      username
    }
  }
  followers {
    id
    user {
      id
      username
    }
  }
  created
}